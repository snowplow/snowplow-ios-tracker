name: Build

on: [push]

jobs:
  test:
    runs-on: macos-latest

    strategy:
      fail-fast: false
      matrix:
        ios-version:
          - 13.4

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup environment
        run: |
          .scripts/setup.sh

      - name: Test readable env var
        run: |
          echo ${{ env.BUILD_TEST1 }}
          echo ${{ env.CI_TEST1 }}
